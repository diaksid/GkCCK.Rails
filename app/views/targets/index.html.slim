- cache @models do
  ruby:
    size = {}
    if @model.attach.attached?
      size = {width: 192, height: 144} if @model.attach.metadata[:width] > @model.attach.metadata[:height]
      size = {width: 144, height: 192} if @model.attach.metadata[:width] < @model.attach.metadata[:height]
    end
    locals = {class: ['c-img--response',
                      'h-width--100'],
              fit: true,
              lightbox: '%s-lightbox' % action_name,
              links: true,
              wrapper: {class: ['mdl-cell',
                                'mdl-cell--3-col-desktop']}}

  .pro-content
    .catalog.catalog--striped = render @models
